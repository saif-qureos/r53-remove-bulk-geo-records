{
  "name": "r53-remove-bulk-geo-records-action",
  "version": "1.0.0",
  "description": "This GitHub Action removes the geolocation records of a domain in AWS Route 53 and laves only the default one, using AWS CLI.\r ## Inputs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@aws-sdk/client-route-53": "^3.282.0"
  }
}
